import { AccordionComponent } from '../ui/accordion'

export const FunctionsComponent = () => {
	return (
		<AccordionComponent
			title="Functions"
			list={[{ name: 'coming soon', id: 0, onClick: () => {} }]}
		/>
	)
}
